---
title: Brick catalogue
description: Every brick Oven ships — what it does, what it adds, and whether it is built yet.
sidebar:
  order: 0
  label: Overview
---

import { Aside } from '@astrojs/starlight/components'

**Add a brick, get a feature.** Each one contributes a capability to the request context, fully
typed — and leaving it out makes using it a compile error rather than a crash at 3am.

Every page below answers the same questions in the same order: what it installs, what files and
tables it creates, what endpoints it adds, and what it cannot do. If a brick has no endpoints,
its page says so rather than omitting the section.

## Shipped

| Brick | Adds | Endpoints |
| --- | --- | --- |
| [`openapi`](/docs/bricks/openapi/) | `ctx.openapi` | `/openapi.json`, `/docs` |
| [`db`](/docs/bricks/db/) | `ctx.db` — the native client | optional health path |
| [`db-drizzle`](/docs/bricks/db-drizzle/) | `ctx.db` — Drizzle, typed from your schema | none |
| [`db-mongoose`](/docs/bricks/db-mongoose/) | `ctx.db` — a Mongoose `Connection` | none |
| [`auth`](/docs/bricks/auth/) | `ctx.user`, `ctx.auth` | none itself |
| [`auth-basic`](/docs/bricks/auth-basic/) | email and password auth, on Drizzle | eight, at `/auth/*` |
| [`auth-mongo`](/docs/bricks/auth-mongo/) | the same flows, on Mongoose | eight, at `/auth/*` |
| [`auth-clerk`](/docs/bricks/auth-clerk/) | `ctx.user` from Clerk, hosted sign-in | **none** |
| [`auth-better`](/docs/bricks/auth-better/) | `ctx.user` from better-auth | **all of its own** |
| [`storage`](/docs/bricks/storage/) | `ctx.storage` — S3, R2, MinIO, or a local directory | none |
| [`mail`](/docs/bricks/mail/) | `ctx.mail` | `/_oven/mail` in development |
| [`queue`](/docs/bricks/queue/) | `ctx.queue` — jobs, retries, cron | `/_oven/queue` in development |

## Planned

| Brick | Adds |
| --- | --- |
| `@theoven/db-prisma` | `ctx.db` — a `PrismaClient` |

<Aside type="note" title="Bricks are not middleware">
  CORS, rate limiting, compression and security headers ship **in core** as
  [middleware](/docs/reference/middleware/), because they wrap a request rather than add a
  capability. A brick is the other thing: it puts something new on the context.
</Aside>

## Writing your own

The brick contract is small and public — see [Writing a brick](/docs/reference/bricks/) for the
interface and how types flow. A third-party brick is a normal npm package that exports a factory function;
nothing about the first-party ones is privileged.
